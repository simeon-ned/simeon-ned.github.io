# Simeon Nedelchev

Personal academic page: [simeon-ned.github.io](https://simeon-ned.github.io/)

Static HTML — edit `index.html`, `cv.html`, or `teaching.html` directly.

## Presentations (Reveal.js)

Job-application slide decks live in `presentations/`. Copy `presentations/template/` for each role or company — see [presentations/README.md](presentations/README.md).

## CV (LaTeX)

Source: `cv/cv.tex` — compile and copy to the site:

```bash
cd cv && make
```

Requires `latexmk` (TeX Live). Output: [`pdf/CV.pdf`](pdf/CV.pdf).
